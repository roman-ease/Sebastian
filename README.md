# Sebastian — AI Work Supporter

> 有能な執事のように、あなたの業務を静かに整えます。

業務中に残した雑多なメモを、AIが日報・タスク・週報へ自動整理するローカル常駐型デスクトップアプリです。

---

## 概要

情報システム担当・社内兼務職など、**複数チャネルから突発的なタスクが飛び込む業務環境**向けに設計されています。

記録の入口は極限まで軽く。整理・清書・候補提示はセバスチャンが行います。

```
日中: グローバルショートカットでメモを随時記録
終業: 「1日を締める」で日報案を自動生成 → 確認・承認
     → タスク候補をレビューしてタスク一覧へ反映
週末: 週報案を自動生成 → 確認・承認
```

---

## 主な機能

### メモ
- デイリーメモへの自由入力（構造化不要）
- 1秒デバウンスによる自動保存
- グローバルショートカット（`Ctrl+Shift+M`）でどこからでも即起動
- **過去日・翌日ナビゲーション**（← → で日付を移動、過去日のメモも編集可能）
- 未整理メモの可視化（日報未作成時にバナー表示）

### 日報
- メモ＋タスク変更ログをもとにAIが日報案を生成
- 編集可能なドラフト → 承認で `Nippo_yyyymmdd.md` として保存
- 保存先フォルダは設定で変更可能

### タスク管理
- タスクの作成・編集・完了・保留・削除
- **開始日・終了日**の期間指定
- **進捗率**（0〜100%）の手動入力 / チェックリスト連動で自動反映
- **チェックリスト**（複数項目の追加・チェック・削除）
- **作業ノート**（タスクカードから随時追記、インライン編集）
- **変更履歴**のタイムライン表示（ステータス変更・優先度変更・日付変更など）
- **一括操作**（複数選択 → ステータス変更・アーカイブ）
- AIがメモ・日報から新規タスク候補・更新候補を提案
- 差分プレビュー（変更前 → 変更後を項目ごとに表示）
- 反映前にユーザー確認必須（自動確定なし）
- 全操作を監査ログに記録（手動操作 / AI提案を区別）

### AI × チェックリスト・進捗
- **チェックリスト自動生成**: タスクのタイトル・概要からAIが5〜7項目を提案
- **AI代理チェック**: 今日のメモとチェックリスト項目を照合し、完了済み項目を提案・確認後まとめてチェック
- **AI進捗コメント**: 進捗率・チェックリスト状況をもとにセバスチャンが執事口調で一言
- **チェックリスト連動**: チェック完了数から進捗率をワンクリックで算出・反映

### AI プロバイダー
設定画面でプロバイダーをいつでも切り替え可能。

| プロバイダー | 種別 | 特徴 |
|---|---|---|
| **Gemini API** | クラウド | 推奨・無料枠あり |
| **Claude**（Anthropic） | クラウド | 高精度・長文対応 |
| **OpenAI** | クラウド | GPT-4o 等 |
| **Groq** | クラウド | 無料枠あり・超高速推論 |
| **OpenRouter** | クラウド | 多モデル対応・無料枠あり |
| **LM Studio** | ローカル | OpenAI互換 GUI ツール |
| **Ollama** | ローカル | CLI ベースのローカルLLM |
| **カスタム** | 任意 | OpenAI互換 / Claude互換 エンドポイントを自由登録 |

### 検索
- **グローバル検索**（`/search`）でタスク・メモ・日報を横断検索
- 300msデバウンスでリアルタイム絞り込み
- 結果をタスク・メモ・日報の3セクションに分けて表示

### 週カレンダー
- 月曜始まりの週表示
- **期間タスクをスパンバー**で表示（開始日〜終了日を横断するバー、前後週への延長も表示）
- スパンバー・単日チップに進捗率・チェックリスト達成数（x/n）を表示
- 各曜日カラム下端に**タスク負荷バー**（タスク数に応じて伸縮・変色）
- カレンダー下部に切り替え可能なパネル
  - **週のサマリー**: 期日タスク合計・完了数・未完了数・高優先度数・達成率バー・週評AIコメント
  - **今週の格言**: 著名人の名言を週番号でローテーション表示

### 週報
- 週内の日報＋タスクログからAIが週報案を生成
- 承認で `Shuho_yyyymmdd.md` として保存

### 朝のブリーフィング
- アプリ起動時（1日1回）に当日期日・期限近・優先度高タスクを表示
- タスクごとの**進捗率バー**・**チェックリスト達成数**も合わせて表示

### ダッシュボード
- 今日が期日・高優先度・ピン留めタスクを一覧表示
- 各タスクに**進捗率バー**・**チェックリスト達成数**を表示
- カテゴリ別の進捗サマリー

### 終業リマインド
- 指定時刻にアプリ通知で「日報を締めませんか？」を通知（Web Notification API使用）
- 通知クリックで日報ページへ直接遷移
- 時刻・平日のみ通知を設定で変更可能
- 初回起動時にブラウザの通知許可が必要です

### データ同期
- **クラウド同期（Supabase）**: Windows / Mac / Android / iOS でタスク・メモ・日報・週報を自動同期
  - 設定画面で Project ID・Publishable Key を入力するだけで有効化
  - 書き込み時即時 push・起動時および1分ごとに pull
  - 未設定時はローカルのみで動作（従来どおり）
- **フォルダ同期**: OneDrive・USB など共有フォルダ経由でのPC間エクスポート／インポート

### Quill 連携
- 指定フォルダに当日メモを `YYYY-MM-DD.md` として書き出し
- Quill でそのファイルを開くと双方向リアルタイム同期

---

## UI デザイン

クラシカル書斎をモチーフにした独自テーマを採用しています。

- **フォント**: Cinzel（見出し） / EB Garamond（本文）
- **3テーマ対応**: ライト（パーチメント）/ ダーク（夜の書斎）/ セピア（アンティーク）
  - サイドバー内のトグルでいつでも切り替え可能
- **執事イラスト**: サイドバー下部に常駐し、アプリの世界観を演出

---

## 技術スタック

| 領域 | 技術 |
|------|------|
| フレームワーク | [Tauri v2](https://tauri.app/) + [React 19](https://react.dev/) |
| 言語 | TypeScript / Rust |
| UI | Tailwind CSS v4 |
| DB | SQLite（`tauri-plugin-sql`） |
| クラウド同期 | Supabase（オプション） |
| AI | Gemini / Claude / OpenAI / Groq / OpenRouter / Ollama / LM Studio / カスタム |
| ビルドツール | Vite |

---

## セットアップ

### 必要環境

- [Node.js](https://nodejs.org/) 18以上
- [Rust](https://www.rust-lang.org/tools/install)（`rustup` 経由推奨）
- [Tauri CLI prerequisites](https://tauri.app/start/prerequisites/)（Windows: WebView2 / Build Tools）

### インストール・起動

```bash
# 依存パッケージのインストール
npm install

# 開発サーバー起動（HMR有効）
npm run tauri dev

# プロダクションビルド
npm run tauri build
```

### macOS版（DMG）のインストール

[Releases](https://github.com/roman-ease/Sebastian/releases) から `Sebastian_x.x.x_aarch64.dmg` をダウンロードし、`Sebastian.app` を `アプリケーション` フォルダにドラッグしてください。

本アプリは Apple Developer 証明書による署名・公証を行っていないため、初回起動時に macOS の Gatekeeper により **「"Sebastian" は壊れているため開けません」** または **「開発元を確認できないため開けません」** と表示されます。

起動するには、ターミナルで以下を実行して quarantine（検疫）属性を削除してください。

```bash
xattr -cr /Applications/Sebastian.app
```

その後、通常どおりダブルクリックで起動できます。

> ⚠️ **免責事項**
> 上記のコマンドは、macOS が未署名アプリに対して行う保護（Gatekeeper）を本アプリに限って解除するものです。
> 実行は**自己責任**でお願いします。本アプリの利用により生じたいかなる損害についても、作者は責任を負いません。
> 心配な場合は、ソースコードを確認のうえ、上記の「インストール・起動」の手順でご自身でビルドすることを推奨します。

### AI設定

アプリ起動後、**設定 → AI設定** から使用するプロバイダーを選択してください。

**Gemini API（推奨・無料）**
1. [Google AI Studio](https://aistudio.google.com/apikey) で無料APIキーを取得
2. 設定画面でAPIキーとモデル（`gemini-2.0-flash`）を入力

**Claude / OpenAI / Groq / OpenRouter（クラウド）**
1. 各サービスのコンソールでAPIキーを取得
2. 設定画面でプロバイダーを選択し、APIキーとモデルIDを入力

**Ollama / LM Studio（ローカルLLM）**
1. [Ollama](https://ollama.com) または [LM Studio](https://lmstudio.ai) をインストール
2. モデルをダウンロード（例: `ollama pull qwen2.5:7b`）
3. 設定画面でエンドポイントURLとモデル名を入力

**カスタムプロバイダー**
- 設定画面の「カスタムプロバイダー」セクションから OpenAI互換 / Claude互換 エンドポイントを自由に登録できます

> ⚠️ **APIキーの取り扱いについて**
> APIキーはローカルDBに平文で保存されます。
> 共有PCや管理外端末への導入は推奨しません。

---

## データの保存場所

| データ | 保存先 |
|--------|--------|
| メモ・タスク・日報・週報（DB） | `%APPDATA%\com.sebastian.app\sebastian.db` |
| 日報 Markdown ファイル | 設定で指定したフォルダ |
| 週報 Markdown ファイル | 設定で指定したフォルダ |
| クラウド同期（オプション） | Supabase（設定で Project ID・Key を指定） |

---

## 設計方針

- **ローカルファースト** — データは手元の SQLite に保存。クラウド同期はオプション。
- **AI提案は必ずユーザー確認を経由** — 自動確定処理は一切行わない。
- **AI失敗時もデータを守る** — メモ・タスクは常に手動で管理可能。
- **軽量さ優先** — 常駐しても邪魔にならない起動速度を維持。

---

## バージョン履歴

### v1.3.2 — 2026-07-15

#### 改善
- **クラウド同期の認証対応（Supabase Auth）**: 設定に認証メールアドレス／パスワード欄を追加。テーブルを RLS（authenticated 本人限定ポリシー）で保護した構成に対応し、同期前に自動サインインする。パスワードは OS キーチェーンに保管（要: Supabase 側の Auth ユーザー作成と RLS 有効化。手順は `docs/supabase-auth-rls.md`）
- **同期失敗の可視化**: push/pull の失敗をサイドバー下部に「クラウド同期に失敗しています」として表示（従来はコンソールのみで気づけなかった）
- タスク詳細ポップアップを Escape キーで閉じられるように
- テスト基盤（vitest）を導入し、時刻比較・AI 応答パースの単体テストを追加

#### バグ修正
- **同期の時刻比較を修正**: ローカル更新時刻（UTC）が端末のタイムゾーンで解釈され、JST では直近9時間のローカル編集がリモートに上書きされ得た問題を修正
- pull 時の通信・権限エラーが無視されていた問題を修正（エラー検知して表示に反映）
- カスタムプロバイダーの API キーが平文で DB に残っていた問題を修正（OS キーチェーンへ自動移送）

---

### v1.3.1 — 2026-07-09

#### 改善
- **タスク一覧から詳細ポップアップを表示**: タイトルクリックでタスク詳細（チェックリスト連動進捗・AI チェックリスト生成・AI 代理チェック・AI 進捗コメント・変更履歴）を開けるように。フィールド編集は行ホバーの鉛筆ボタンから
- **一括選択の強化**: リスト上部に「表示中の N 件を選択」を追加。絞り込み（ステータス・カテゴリ・検索）結果に対する全選択 → 一括ステータス変更／一括アーカイブが可能に。選択は表示中の行に限定され、見えていないタスクへの誤操作を防止
- **詳細ポップアップから編集モーダルへ切替**: 右上の鉛筆ボタンで編集モーダルを重ねて表示。ダッシュボード・週次ビューからも編集可能に
- ダッシュボード・週次ビューで詳細ポップアップを閉じた際に表示を再読込

#### バグ修正
- **同期の堅牢化（Supabase）**:
  - タスク削除をトゥームストーン方式に変更し、他端末で削除済みタスクが復活する問題を修正（要: `tasks` テーブルへの `deleted_at` 列追加）
  - チェックリストの pull がリモート優先で上書きし、未同期のローカル変更が消える問題を修正
  - 差分 pull 化（前回以降の更新行のみ取得）と pull の多重実行ガードを追加
  - メモ編集中に pull が走ると入力内容が巻き戻る問題を修正（編集中ガード）
- **API キーの保存を OS キーチェーンへ移行**: 平文 SQLite 保存をやめ、Windows 資格情報マネージャー / macOS Keychain に保存（起動時に既存キーを自動移送）
- AI の JSON 応答パースを堅牢化（コードフェンス等を除去してから解析）
- 詳細ポップアップでの進捗率・ノート・チェックリスト変更が同期・変更履歴に記録されない問題を修正
- 一括ステータス変更・一括アーカイブが変更履歴・同期に記録されない問題を修正

---

### v1.3.0 — 2026-05-08

#### 新機能
- **Supabase マルチデバイス同期**: Windows / Mac / Android / iOS で同一データを共有
  - タスク・チェックリスト・メモ・日報・週報を自動同期
  - 書き込み時即時 push、起動時および1分ごとに pull
  - 設定画面で Project ID・Publishable Key を入力するだけで有効化
  - 「ローカルDB を Supabase にインポート」ボタンで既存データを一括移行
  - 未設定時は同期をスキップ（従来通りローカルのみで動作）
- **初期セットアップガイド**: 設定画面に手順書ポップアップを追加
  - AI プロバイダー（Gemini / Groq / Claude / OpenAI）の API キー取得手順
  - ローカル LLM（Ollama / LM Studio）のインストール・起動手順
  - Supabase のアカウント作成からテーブル構築（コピペ用 SQL 付き）・Key 入力まで
- **Quill 連携**: 指定フォルダに当日メモを `YYYY-MM-DD.md` として書き出し（Quill でのリアルタイム編集に対応）

---

### v1.2.2 — 2026-05-08

#### バグ修正
- OpenAI / Groq / OpenRouter / LM Studio の接続テストが常に「成功」を返す偽テストになっていた問題を修正（実APIリクエストに変更）
- カスタムプロバイダーの接続テストも実APIリクエストに対応（OpenAI互換・Claude互換それぞれの形式で送信）
- ネットワーク接続失敗時（`Load Failed` 等）にプロバイダー・エンドポイント別の日本語案内メッセージを表示するよう改善

#### 新機能
- **ローカルLLMのワンクリック起動**: 設定画面の Ollama / LM Studio 選択時に「サーバーを起動する」ボタンを追加。起動後に接続テストを自動実行（3秒待機）
  - Ollama: macOS では `Ollama.app` を起動、その他は `ollama serve` をバックグラウンド実行
  - LM Studio: `lms` CLI がある場合は `lms server start`、なければ `LM Studio.app` を起動

---

### v1.2.1 — 2026-04-30

#### バグ修正
- ダーク / セピアテーマで週カレンダーの日付セル背景が白く浮いて見える問題を修正（`white` ハードコードを CSS 変数 `--color-sebastian-card` に変更）

---

### v1.2.0 — 2026-04-28

#### タスク機能の大幅拡張
- **開始日・終了日**: 期日のみから期間指定に変更。週カレンダーにスパンバーで表示
- **進捗率**: 0〜100% の手動入力。チェックリストの達成率を連動反映するボタン追加
- **チェックリスト**: 複数項目の追加・チェック・削除。TaskModalとPeekModal両方から管理可能
- **作業ノート**: PeekModalからインライン編集。TaskModalにも専用フィールド追加
- **変更履歴**: 各タスクの操作ログをタイムライン表示（PeekModal最下部）
- **一括操作**: 複数タスクを選択してステータス変更・アーカイブ

#### AI × チェックリスト・進捗
- **チェックリスト自動生成**: タイトル・概要からAIが5〜7項目を提案・一括追加
- **AI代理チェック**: 今日のメモとチェックリストを照合し、完了済み項目を提案
- **AI進捗コメント**: 進捗・チェックリスト状況をもとにセバスチャンが執事口調で一言
- **チェックリスト連動進捗率**: `🔗` ボタンでチェック達成数から進捗率を即算出・保存

#### 週カレンダー
- **期間タスク スパンバー**: 開始日〜終了日をまたぐ横断バーを追加（前後週延長は ◀▶ で表示）
- スパンバー・単日チップに進捗率・チェックリスト達成数（x/n）を表示
- 列ラッパー方式によるグリッドレイアウトの刷新（行境界の不要ラインを完全除去）

#### AI プロバイダー拡張
- **Claude / OpenAI / Groq / OpenRouter / LM Studio** を新規追加（計8プロバイダー）
- **カスタムプロバイダー**: OpenAI互換・Claude互換エンドポイントを DB 登録・管理
- `callOpenAICompatible()` ヘルパーで複数プロバイダーを統一処理
- **DB Migration 8**: `custom_providers` テーブルを追加

#### その他
- **グローバル検索**: タスク・メモ・日報を横断検索（サイドバーから `/search` へ）
- **ダッシュボード**: 各タスクに進捗率バー・チェックリスト達成数を表示
- **朝のブリーフィング**: 各タスクに進捗率・チェックリスト状況を表示
- **メモ**: 過去日・翌日ナビゲーションを追加
- **DB Migration 5〜8**: `start_date` / `progress` / `task_checklist` / `custom_providers` を追加

---

### v1.1.2 — 2026-04-27

#### バグ修正
- Gemini リトライ最終試行後に適切なエラーメッセージを表示するよう修正
- Settings 画面のトグルボタン（Tailwind v4対応）を修正

---

### v1.1.1 — 2026-04-03

#### バグ修正・細部改善
- デモモードでタスク一覧を開くと画面が白くなる不具合を修正（デモデータに `created_at` / `updated_at` が欠落していたためソートがクラッシュ）
- 週スケジュール — 週評AIコメントの再生成ボタンを追加

---

### v1.1.0 — 2026-04-03

#### タスク一覧 — ソート・フィルター機能強化
- キーワード検索（タイトル・カテゴリ・説明文）
- カテゴリフィルター（登録済みカテゴリをドロップダウンで絞り込み）
- ソートキー選択（作成日 / 更新日 / 期日 / 優先度）・昇降順切り替え
- 件数表示（絞り込み中は「N 件 / M 件中」を表示）

#### 執事コメント
- サイドバーの執事イラストをクリックすると吹き出しでコメントを表示
- 時間帯別（朝 / 昼 / 夕方 / 夜）に5コメントずつ、AI生成で用意（1日1回キャッシュ）

#### 週スケジュール — サマリーパネル追加
- 各曜日カラム下端にタスク負荷バーを追加
- カレンダー下部に週のサマリー・今週の格言パネルを追加

#### 日報生成 — 品質改善
- Gemini API の `maxOutputTokens` を 2048 → 8192 に引き上げ

---

### v1.0.0 — 2026-03-31

初回リリース。

- **メモ**: デイリーメモへの自由入力・自動保存・グローバルショートカット
- **日報**: メモ＋タスクログからAIが日報案を生成、承認でMarkdown保存
- **タスク管理**: 作成・編集・完了・保留・アーカイブ・ピン留め、AIによる候補提案
- **週カレンダー**: 月曜始まりの週表示、期日付きタスクの表示
- **週報**: 今週の日報＋タスクログからAIが週報案を生成、承認でMarkdown保存
- **朝のブリーフィング**: 起動時に優先度・期日の高いタスクを案内
- **終業リマインド**: 指定時刻にデスクトップ通知
- **データ同期**: 指定フォルダへのエクスポート／インポート
- **テーマ**: ライト / ダーク / セピアの3テーマ対応

---

## ライセンス

MIT
